# OPS - Система почасового учета рабочей силы

Веб-приложение для почасового учета рабочей силы в разных компаниях с ролевой моделью доступа, сводной статистикой и системой задач.

## Технологический стек

- **Frontend**: React + TypeScript + Vite + Tailwind CSS
- **Backend**: Node.js + Express + TypeScript
- **База данных**: SQLite (совместимо с PostgreSQL)
- **Аутентификация**: Сессионная аутентификация

## Установка и запуск

### 🐳 Docker (Рекомендуется)

Самый простой способ запуска - через Docker:

```bash
# 1. Скопируйте .env.example в .env и настройте переменные
cp .env.example .env

# 2. (Опционально) Если порты заняты, измените их в .env:
# FRONTEND_PORT=3001
# BACKEND_PORT=5001
# ANALYZ_PORT=5051

# 3. Запустите все сервисы
docker-compose up -d

# 4. Проверьте статус
docker-compose ps
```

Приложение будет доступно (порты можно изменить в `.env`):
- Frontend: http://localhost:${FRONTEND_PORT:-3000}
- Backend API: http://localhost:${BACKEND_PORT:-5000}
- Analyz: http://localhost:${ANALYZ_PORT:-5050}

Подробные инструкции по Docker см. в [DOCKER.md](DOCKER.md)

### 📦 Локальная установка

#### Предварительные требования

- Node.js (версия 18 или выше)
- Python 3.8+ (для Analyz сервиса)
- npm или yarn

#### Установка зависимостей

Все зависимости устанавливаются автоматически через Docker. Никаких дополнительных действий не требуется.

#### Запуск приложения

**Рекомендуемый способ - через Docker:**

```bash
# 1. Настройте переменные окружения
cp .env.example .env

# 2. Запустите все сервисы
docker-compose up -d

# 3. Проверьте статус
docker-compose ps
```

Приложение будет доступно:
- Frontend: http://localhost:3000
- Backend API: http://localhost:5000
- Analyz: http://localhost:5050

Подробные инструкции см. в [DOCKER.md](DOCKER.md)

**Локальный запуск (для разработки):**

Если вы хотите запустить без Docker, установите зависимости вручную:

```bash
# Backend
cd backend
npm install
npm run dev

# Frontend (в другом терминале)
cd frontend
npm install
npm run dev

# Analyz (в третьем терминале)
cd Analyz
pip install -r requirements.txt
python app.py
```

## Тестовые аккаунты

После первого запуска база данных автоматически инициализируется со следующими пользователями:

- **Администратор**: `admin` / `admin123`
- **Оператор**: `operator` / `operator123`
- **Менеджер Мувинг**: `manager_muving` / `muving123`
- **Менеджер ЭСК**: `manager_esk` / `esk123`
- **Менеджер Градусы**: `manager_gradusy` / `gradusy123`
- **Менеджер 2колеса**: `manager_2kolesa` / `2kolesa123`

## Функциональность

### Роли пользователей

#### Администратор (Admin)
- Полный доступ ко всем данным и пользователям
- CRUD операции для пользователей
- Просмотр и редактирование данных в сводной таблице (инлайн-редактирование)
- Создание задач для менеджеров
- Drag-and-drop для блоков на главной странице
- Настройки сохраняются в LocalStorage

#### Оператор (Operator)
- Просмотр всех данных
- Доступ к сводной таблице
- Создание задач для менеджеров
- Drag-and-drop для блоков

#### Менеджер (Manager)
- Привязан к одной из 4 компаний: Мувинг, ЭСК, Градусы, 2колеса
- Экран ввода данных для своей компании
- Ввод данных по операциям и часам (10:00 - 21:00)
- Просмотр задач для своей компании
- Загрузка фото для задач
- Отметка задач как выполненных

### Операции

Система отслеживает следующие операции:
- Комплектация
- Размиксовка
- Уборка Холод
- Уборка Сухой
- Пресс
- Уборка Паллет
- Отгрузка паллет
- Подвоз РК
- Замотка РК

### Компании

- Мувинг
- ЭСК
- Градусы
- 2колеса

## Структура проекта

```
OPS/
├── frontend/              # React приложение
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── pages/        # Страницы приложения
│   │   ├── contexts/     # React контексты
│   │   └── constants.ts  # Константы
│   ├── Dockerfile        # Production Dockerfile
│   ├── Dockerfile.dev    # Development Dockerfile
│   └── package.json
├── backend/              # Express сервер
│   ├── src/
│   │   ├── routes/       # API маршруты
│   │   ├── middleware/   # Middleware
│   │   ├── database.ts   # База данных
│   │   └── index.ts      # Точка входа
│   ├── Dockerfile        # Production Dockerfile
│   ├── Dockerfile.dev    # Development Dockerfile
│   └── package.json
├── Analyz/               # Flask сервис (аналитика)
│   ├── modules/          # Модули приложения
│   ├── templates/        # HTML шаблоны
│   ├── static/           # Статические файлы
│   ├── data_days/        # Данные по дням
│   ├── Dockerfile        # Dockerfile
│   └── requirements.txt  # Python зависимости
├── docker-compose.yml    # Production Docker Compose
├── docker-compose.dev.yml # Development Docker Compose
├── .env.example          # Пример переменных окружения
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь

### Пользователи (только Admin)
- `GET /api/users` - Список пользователей
- `GET /api/users/:id` - Получить пользователя
- `POST /api/users` - Создать пользователя
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

### Компании
- `GET /api/companies` - Список компаний

### Почасовые данные
- `GET /api/hourly-data/:hour` - Данные за час
- `GET /api/hourly-data/summary/:hour` - Сводка за час
- `POST /api/hourly-data` - Сохранить данные
- `PUT /api/hourly-data/bulk` - Массовое обновление (Admin)

### Задачи
- `GET /api/tasks` - Список задач
- `POST /api/tasks` - Создать задачу
- `PATCH /api/tasks/:id/status` - Обновить статус
- `PATCH /api/tasks/:id/photo` - Обновить фото

### Загрузка файлов
- `POST /api/upload` - Загрузить фото

## Особенности

- Автоматическое сохранение данных при вводе
- Проверка истечения времени по задачам
- Drag-and-drop для перестановки блоков
- Скрытие/показ блоков с сохранением в LocalStorage
- Инлайн-редактирование данных в сводной таблице
- Загрузка и просмотр фото для задач
- Таймлайн для навигации по часам

## Разработка

### Сборка для продакшена

**Через Docker (рекомендуется):**
```bash
# Сборка всех образов
docker-compose build

# Или пересборка без кэша
docker-compose build --no-cache
```

**Локальная сборка:**
```bash
# Backend
cd backend
npm run build

# Frontend
cd frontend
npm run build
```

### Development режим

Для разработки с hot reload используйте:
```bash
docker-compose -f docker-compose.dev.yml up
```

Это запустит все сервисы с автоматической перезагрузкой при изменении кода.

## Документация

- **🐳 Docker**: см. [DOCKER.md](DOCKER.md) - инструкции по запуску в Docker
- **📦 Установка**: см. [INSTALL.md](INSTALL.md) - локальная установка
- **🚀 Деплой**: см. [DEPLOY.md](DEPLOY.md) - деплой на сервер
- **🔧 Решение проблем**: см. [DEPLOY_TROUBLESHOOTING.md](DEPLOY_TROUBLESHOOTING.md)
- **🏗️ Архитектура Analyz**: см. [Analyz/ARCHITECTURE.md](Analyz/ARCHITECTURE.md)

## Быстрый деплой на сервер

**Через Docker (рекомендуется):**

```bash
# 1. На сервере установите Docker и Docker Compose
# 2. Скопируйте проект на сервер
# 3. Настройте .env файл
cp .env.example .env

# 4. Запустите
docker-compose up -d
```

Подробные инструкции по деплою см. в [DEPLOY.md](DEPLOY.md)

## Лицензия

ISC

