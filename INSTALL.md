# Инструкция по установке OPS

## Системные требования

- **Node.js**: версия 18 или выше
- **npm**: версия 9 или выше (устанавливается вместе с Node.js)
- **Python**: версия 3.8 или выше (для модуля Analyz)
- **Операционная система**: Windows, Linux или macOS

## Установка Node.js и npm на Linux

### Рекомендуемый способ (NodeSource)

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверка установки
node --version
npm --version
```

### Альтернативный способ (через apt)

```bash
# Установка Node.js и npm из репозиториев
sudo apt update
sudo apt install -y nodejs npm

# Проверка версий (может быть старая версия)
node --version
npm --version
```

**Примечание**: Установка через `apt install npm` может установить устаревшую версию. Рекомендуется использовать NodeSource для получения актуальной версии Node.js 18+.

## Быстрая установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd opsWORK
```

### 2. Установка зависимостей

#### Автоматическая установка и запуск (для сервера)

**Python версия (рекомендуется, кроссплатформенная):**
```bash
python3 setup_and_deploy.py
```

**Bash версия (Linux/macOS):**
```bash
chmod +x setup_and_deploy.sh
sudo ./setup_and_deploy.sh
```

Этот скрипт:
- Проверит и установит все системные зависимости (Node.js, Python, npm, pip)
- Установит все зависимости проекта
- Соберет проект (build)
- Запустит проект через PM2

#### Автоматическая установка зависимостей (без запуска)

```bash
npm run install:all
```

Эта команда установит зависимости для:
- Корневого проекта
- Frontend (React приложение)
- Backend (Node.js сервер)

#### Ручная установка

```bash
# Установка зависимостей корневого проекта
npm install

# Установка зависимостей Frontend
cd frontend
npm install
cd ..

# Установка зависимостей Backend
cd backend
npm install
cd ..
```

### 3. Установка Python зависимостей (для Analyz)

```bash
cd Analyz
pip install -r requirements.txt
cd ..
```

**Примечание**: На Linux может потребоваться установка дополнительных системных библиотек:

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install python3-dev python3-pip

# CentOS/RHEL
sudo yum install python3-devel python3-pip
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта (опционально):

```env
# Backend порт (по умолчанию 5000)
BACKEND_PORT=5000

# Frontend порт (по умолчанию 3000)
FRONTEND_PORT=3000

# Секретный ключ для сессий (сгенерируйте случайную строку)
SESSION_SECRET=your-secret-key-here

# Путь к базе данных (по умолчанию backend/database.db)
DATABASE_PATH=backend/database.db
```

### 5. Инициализация базы данных

База данных создается автоматически при первом запуске backend сервера.

## Запуск приложения

### 🐳 Docker (Рекомендуется)

Самый простой способ - использовать Docker:

```bash
# 1. Настройте переменные окружения
cp .env.example .env

# 2. Запустите все сервисы
docker-compose up -d

# 3. Проверьте статус
docker-compose ps
```

Подробные инструкции см. в [DOCKER.md](../DOCKER.md)

### Локальный режим разработки

#### Windows

```bash
start.bat
```

или

```bash
python start.py
```

#### Linux/macOS

```bash
chmod +x start.sh
./start.sh
```

или

```bash
python3 start.py
```

### Ручной запуск

#### Терминал 1 - Backend

```bash
cd backend
npm run dev
```

Backend будет доступен на `http://localhost:5000`

#### Терминал 2 - Frontend

```bash
cd frontend
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

#### Терминал 3 - Analyz (опционально)

```bash
cd Analyz
python app.py
```

Analyz будет доступен на `http://localhost:5001`

### Фоновый запуск (Linux/macOS)

```bash
python start_background.py
```

## Проверка установки

После запуска откройте браузер и перейдите на:

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5000/api/auth/me

### Тестовые аккаунты

После первого запуска создаются следующие пользователи:

- **Администратор**: `admin` / `admin123`
- **Оператор**: `operator` / `operator123`
- **Менеджер Мувинг**: `manager_muving` / `muving123`
- **Менеджер ЭСК**: `manager_esk` / `esk123`
- **Менеджер Градусы**: `manager_gradusy` / `gradusy123`
- **Менеджер 2колеса**: `manager_2kolesa` / `2kolesa123`

**Важно**: Измените пароли после первого входа!

## Решение проблем

### Node.js не найден

Установите Node.js с официального сайта: https://nodejs.org/

Проверьте установку:
```bash
node --version
npm --version
```

### Ошибки при установке зависимостей

Очистите кэш npm и переустановите:

```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Ошибки с Python модулями

Убедитесь, что установлен Python 3.8+:

```bash
python --version
# или
python3 --version
```

### Порт уже занят

Измените порты в `.env` файле или остановите процесс, использующий порт:

**Windows:**
```bash
netstat -ano | findstr :5000
taskkill /PID <PID> /F
```

**Linux/macOS:**
```bash
lsof -ti:5000 | xargs kill -9
```

## Структура проекта

```
opsWORK/
├── frontend/          # React приложение
│   ├── src/          # Исходный код
│   ├── dist/         # Собранное приложение (после build)
│   └── package.json
├── backend/          # Node.js сервер
│   ├── src/          # Исходный код TypeScript
│   ├── dist/         # Скомпилированный JavaScript (после build)
│   ├── uploads/      # Загруженные файлы
│   └── package.json
├── Analyz/           # Python модуль анализа данных
│   ├── app.py        # Flask приложение
│   ├── data_days/    # Данные по дням
│   └── requirements.txt
├── start.py          # Скрипт запуска
├── start.sh          # Скрипт запуска (Linux/macOS)
├── start.bat         # Скрипт запуска (Windows)
└── package.json      # Корневой package.json
```

## Дополнительная информация

- Подробная документация API: см. `README.md`
- Инструкции по деплою: см. `DEPLOY.md`
- Архитектура проекта: см. `Analyz/ARCHITECTURE.md`

